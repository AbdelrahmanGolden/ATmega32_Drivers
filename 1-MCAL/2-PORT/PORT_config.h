/***************************************************************************/
/***************************************************************************/
/***********************	Author  : Golden		************************/
/***********************	Layer   : MCAL			************************/
/***********************	SWC     : PORT			************************/
/***********************	Version : 1.00			************************/
/***********************	Date    : 29/8/2024		************************/
/***************************************************************************/
/***************************************************************************/

/**		File Guard		**/
#ifndef PORT_CONFIG_H_
#define PORT_CONFIG_H_


/***		PORTA Direction	and Initial Value		***/
#define PORTA_PIN0_DIR 					0		/* Choose 0 for input, 1 for output. */
#define PORTA_PIN1_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTA_PIN2_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTA_PIN3_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTA_PIN4_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTA_PIN5_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTA_PIN6_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTA_PIN7_DIR					0		/* Choose 0 for input, 1 for output. */

#define PORTA_PIN0_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTA_PIN1_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTA_PIN2_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTA_PIN3_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTA_PIN4_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTA_PIN5_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTA_PIN6_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTA_PIN7_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */


/***		PORTB Direction	and Initial Value		***/
#define PORTB_PIN0_DIR 					0		/* Choose 0 for input, 1 for output. */
#define PORTB_PIN1_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTB_PIN2_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTB_PIN3_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTB_PIN4_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTB_PIN5_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTB_PIN6_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTB_PIN7_DIR					0		/* Choose 0 for input, 1 for output. */

#define PORTB_PIN0_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTB_PIN1_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTB_PIN2_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTB_PIN3_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTB_PIN4_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTB_PIN5_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTB_PIN6_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTB_PIN7_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */


/***		PORTC Direction	and Initial Value		***/
#define PORTC_PIN0_DIR 					0		/* Choose 0 for input, 1 for output. */
#define PORTC_PIN1_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTC_PIN2_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTC_PIN3_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTC_PIN4_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTC_PIN5_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTC_PIN6_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTC_PIN7_DIR					0		/* Choose 0 for input, 1 for output. */

#define PORTC_PIN0_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTC_PIN1_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTC_PIN2_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTC_PIN3_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTC_PIN4_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTC_PIN5_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTC_PIN6_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTC_PIN7_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */


/***		PORTD Direction	and Initial Value		***/
#define PORTD_PIN0_DIR 					0		/* Choose 0 for input, 1 for output. */
#define PORTD_PIN1_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTD_PIN2_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTD_PIN3_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTD_PIN4_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTD_PIN5_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTD_PIN6_DIR					0		/* Choose 0 for input, 1 for output. */
#define PORTD_PIN7_DIR					0		/* Choose 0 for input, 1 for output. */

#define PORTD_PIN0_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTD_PIN1_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTD_PIN2_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTD_PIN3_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTD_PIN4_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTD_PIN5_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTD_PIN6_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */
#define PORTD_PIN7_INITIAL_VALUE		0		/* Choose 0 for Floating if input, low if output ... Choose 1 for Pulled up if input, High if output. */




#endif /* PORT_CONFIG_H_ */